name: Auto Send Todo List

on:
  schedule:
    # 毎日朝7:30 JST = 22:30 UTC（前日）
    - cron: '30 22 * * *'
  workflow_dispatch:  # 手動実行用

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create credentials.json
        run: |
          echo '${{ secrets.GMAIL_CREDENTIALS }}' > credentials.json

      - name: Restore token.pickle from secret
        run: |
          echo '${{ secrets.GMAIL_TOKEN_PICKLE }}' > token_b64.txt
          base64 -d token_b64.txt > token.pickle
          rm token_b64.txt

      - name: Run auto-send-todo script
        run: python auto_send_todo_list.py

      - name: Upload token
        if: hashFiles('token.pickle') != ''
        uses: actions/upload-artifact@v4
        with:
          name: token-pickle
          path: token.pickle
          retention-days: 90
